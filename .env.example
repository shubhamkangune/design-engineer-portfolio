PORT=5000
NODE_ENV=development

# The address that will receive contact form messages
CONTACT_EMAIL=shubhamcsc4656@gmail.com

# SMTP configuration (example for Gmail)
# For Gmail use "smtp.gmail.com" and an app-specific password (requires 2FA)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
SMTP_SECURE=false

# Optional: adjust for your provider
# SMTP_SECURE=true for port 465 (SSL)
